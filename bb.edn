{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([redfin-api.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'redfin-api.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
